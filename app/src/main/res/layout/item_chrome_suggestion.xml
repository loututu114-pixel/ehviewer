<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="8dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:minHeight="56dp"
    android:background="?android:attr/selectableItemBackground">

    <!-- Chrome建议类型图标 -->
    <ImageView
        android:id="@+id/suggestion_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="16dp"
        android:src="@drawable/ic_domain"
        android:tint="@color/chrome_text_primary"
        tools:ignore="ContentDescription" />

    <!-- 建议内容区域 -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- 主要文本 -->
        <TextView
            android:id="@+id/suggestion_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textColor="@color/chrome_text_primary"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- 副文本 -->
        <TextView
            android:id="@+id/suggestion_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@color/chrome_text_secondary"
            android:maxLines="1"
            android:ellipsize="end"
            android:visibility="gone" />

    </LinearLayout>

    <!-- 使用建议按钮 -->
    <ImageButton
        android:id="@+id/use_suggestion_button"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="8dp"
        android:src="@drawable/ic_arrow_forward"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:tint="@color/chrome_text_secondary"
        android:visibility="gone"
        tools:ignore="ContentDescription" />

</LinearLayout>
